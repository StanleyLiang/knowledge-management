apiVersion: v1
kind: Secret
metadata:
  name: video-converter-secret
  labels:
    app: video-converter
type: Opaque
stringData:
  # Replace with actual credentials.
  # In production, use sealed-secrets, external-secrets-operator, or vault-injector.
  S3_ACCESS_KEY: "<REPLACE_ME>"
  S3_SECRET_KEY: "<REPLACE_ME>"
